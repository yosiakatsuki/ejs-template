<%
// サイトのルートを定数化.
ROOT = meta[ 'default' ].root;
// サイト内URLの作成.
getUrl = ( path ) => {
    path = path ? `/${ path }` : '';
    path = `${ ROOT.replace( /\/$/, '' ) }${ path }`;
    return path ? path : ROOT;
}
// JS,CSS,アセットに付け加える日付クエリ.
addDateQuery = ( url ) => {
    const sep = -1 === url.indexOf( '?' ) ? '?' : '&';
    const date = new Date();
    const year = date.getFullYear();
    const month = date.getMonth() + 1;
    const day = date.getDate();
    const hours = date.getHours();
    const minutes = date.getMinutes();
    const seconds = date.getSeconds();

    return `${ url }${ sep }${ year }${ month }${ day }${ hours }${ minutes }${ seconds }`;
}
// JS,CSS用URL取得
getScriptsUrl = ( path ) => {
    return addDateQuery( getUrl( path ) );
}
// アセットフォルダURL.
ASSETS = `${ ROOT.replace( /\/$/, '' ) }/assets`;
// アセット内のファイルURL作成.
getAssetUrl = ( path ) => {
    if ( ! path ) {
        return '';
    }

    return addDateQuery( `${ ASSETS }/${ path }` );
}
-%>
